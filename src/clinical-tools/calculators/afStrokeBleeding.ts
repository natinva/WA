import { CLINICAL_DISCLAIMER_TR } from '../constants'; import type { ToolOutput } from '../types';
export function runAfScores(i:any):ToolOutput{ const cha=(i.chf?1:0)+(i.ht?1:0)+(i.age>=75?2:i.age>=65?1:0)+(i.dm?1:0)+(i.stroke?2:0)+(i.vascular?1:0)+(i.sex==='female'?1:0); const has=(i.ht?1:0)+(i.renal?1:0)+(i.liver?1:0)+(i.stroke?1:0)+(i.bleeding?1:0)+(i.inr?1:0)+(i.age>65?1:0)+(i.drugs?1:0)+(i.alcohol?1:0); return {toolId:'cha2ds2-hasbled',resultSummaryTR:`CHA2DS2-VASc ${cha}, HAS-BLED ${has}`,structuredResult:{cha2ds2Vasc:cha,hasBled:has},warningsTR:['Antikoagülan başlama kararı için tek başına kullanılmaz.'],sourceMetadata:[{name:'ESC/score references',type:'formula_reference'}],generatedAt:new Date().toISOString(),disclaimerTR:CLINICAL_DISCLAIMER_TR}; }
