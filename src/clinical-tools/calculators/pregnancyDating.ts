import { addDays, parseDateUTC } from '../utils/date'; import { CLINICAL_DISCLAIMER_TR } from '../constants'; import type { ToolOutput } from '../types';
export const runPregnancyDating=(i:{lmp:string}):ToolOutput=>{ const lmp=parseDateUTC(i.lmp); const edd=addDays(lmp,280); return {toolId:'pregnancy-dating-edd',resultSummaryTR:`Tahmini doğum tarihi ${edd.toISOString().slice(0,10)}`,structuredResult:{edd:edd.toISOString().slice(0,10)},warningsTR:[],sourceMetadata:[{name:'ACOG dating approach',type:'formula_reference'}],generatedAt:new Date().toISOString(),disclaimerTR:CLINICAL_DISCLAIMER_TR}; };
